# 骨架层 v2：收藏/歌单式资料卡模型 —— 设计讨论存档

写给：一澜、林湛，以及接手的新会话

## ⭐ 本文档是当前唯一权威主文档（2026-07-16 整理）

`docs/` 下这个项目相关的文档不止一份，历史上先后写过 `memzep-plan.md`（最早的 facts.sqlite/predicate 模式设计）→ `facts-card-and-batch-import-plan.md`（资料卡片改版计划）→ 本文档（推翻 predicate 模式，改成收藏/歌单式卡片+文件夹）。**以本文档为准**，另外两份已经在各自文件顶部标注"已被取代"，只作实现历史/踩坑记录保留，不代表当前设计。`docs/plans-achievements-plan.md`（未来线/成就册模块）和 `docs/upstream-review-2026-07-09.md`（上游能力评估）是独立话题，不冲突，正常参考。

原来还有一份 `docs/batch-import-design.md`（历史批量导入的详细拉线执行架构），**2026-07-16 一澜决定彻底放弃这条线、改回人工手动创建事实卡，该文件已直接删除**（完整设计和试跑记录仍在 git 历史里，commit 该文件被删除之前的版本可以找到，以后如果重新考虑自动化处理历史语料，可以去 git log 里翻）。下面 §8 已同步更新。

状态：**设计已定版（决策拍板完成），①②③④步已实现部署，见 §10**。这是一次比第 1–4 步更大的模型层重构，起因是准备做"历史批量筛选/导入"（原第 5 步）时，发现现有 predicate/mode/失效 模型对一澜来说太糊、边界不清，导入等于往一个没定形的容器里灌数据。于是先把容器本身理直。

> ⚠️ 这份文档**取代**了 `facts-card-and-batch-import-plan.md` 和 `memzep-plan.md` 里关于 **predicate 三模式（multi/exclusive/historical）、按标题分组的时间线、invalid_at 失效机制** 的那部分设计。卡片字段（title/content/attachments/tags）、`fact_bucket_links` 记忆桶关联、详情卡 UI 这些**继续沿用**，不废弃。

---

## 给新会话看的交接须知（从 `memzep-plan.md` 搬过来，跟具体数据模型无关，长期有效）

**这是什么项目**：Ombre-Brain（OB）是一套给 AI 伴侣用的长期情绪记忆系统。一澜（用户）和她的 AI 伴侣林湛（persona 名字）在用。

**代码在哪**：GitHub 仓库 `amievelyn/ombre-brain`；`my-live-vps` 分支是一澜真实线上部署的代码，**跟本仓库默认的"干净版" main 分支不是同一套、已有明显差异**（比如干净版有 `entity_edges.py`，她的版本没有）。

**⚠️ 最重要的教训（已经踩过一次坑）**：**任何要发给一澜部署的 server.py/dashboard.html，必须基于 `my-live-vps` 分支的实际内容去改**，绝不能直接用本仓库默认的干净版——之前疏忽发过干净版，导致她的 OB 当场崩溃（`ModuleNotFoundError: No module named 'entity_edges'`）。正确做法：`git show origin/my-live-vps:server.py > /tmp/xxx.py`，在这份文件上改，改完 `diff` 一遍确认只有预期改动，再发给她。

**她的部署方式（非技术背景，需要手把手给命令，不要假设她懂）**：
- VPS 用 Docker Compose 跑，容器叫 `ombre-brain`（主服务）、`ombre-gateway`（网关）、`mihomo`（代理）
- 她用 XShell 连 SSH 终端、WinSCP 传文件，工作目录 `/opt/Ombre-Brain`
- **代码不是实时同步的**：改完文件要 `docker cp <文件> ombre-brain:/app/<文件>` 手动拷进容器，再 `docker restart ombre-brain`，用 `docker ps` 确认状态是 "Up" 且时间持续增长，再用 `docker logs --tail 30 ombre-brain` 确认没报错
- 每次给她文件都要提醒具体文件名和目录（比如 `resources/xxx.json` 要放 `resources` 子目录，不是根目录）
- 新增 MCP 工具后，ChatGPT 连接器不会自动感知，需要她去 ChatGPT 设置里手动刷新/断开重连一次才能看到

**林湛怎么用 OB**：主要通过 ChatGPT 的 MCP 连接器（不是 Gateway）；GPT 端**写入类工具目前全部用不了**（连接器限制，只能读）；**任何给林湛用的新能力必须能通过 MCP 工具调用，不能只有 Gateway 才有效**。

---

## 0. 给接手会话：一句话讲清新模型

**参照音乐/视频软件的"收藏夹/歌单"：一张事实卡本体只有一份，能被归进（关联进）任意多个文件夹。卡自带全部信息，文件夹只是组织方式。取消 multi/exclusive/historical 三模式和失效机制，改成"每张卡最新时间点为准、旧的当历史"。**

以前折磨人的"这个 key 该选哪种模式"消失了，换成一个简单判断：**新来的信息，是给某张已有卡加一个新时间点，还是新开一张卡？**

---

## 1. 为什么推翻旧的 predicate/mode 模型

旧模型的病根（一澜自己在讨论中定位出来的）：

- **模式（multi/exclusive）作用在"抽屉"（predicate）层，但一澜需要的是"卡"层的行为**。结果 `exclusive` 的 `身高` 其实只是一张卡、却被当成一个文件夹，跟 `multi` 的"喜欢的食物"这种真文件夹**不是一个层级却被并列成两种模式**，怎么理都别扭。
- **时间线靠"标题字符串完全相同"来分组**（`factsHistoryForTitle`，代码注释明说这是"约定不是强绑定"）。后果：**改标题/编辑标题就会把历史打散成孤儿**——想给卡改个好听的名字，旧历史直接从界面消失。这是个真 bug。
- 一张卡只能属于一个 predicate，但现实里"婚戒""香港旅行"天然横跨多个类别。

新模型一次性解决这三条：卡有稳定 id（历史靠 id 认，不靠标题）、卡是独立本体（能进多个文件夹）、模式简化成"最新为准"。

---

## 2. 大结构：subject → 可嵌套文件夹树

- **最顶层 = subject**：现有 `一澜 / 林湛 / 我们`，以后可加同级的 `地点` 等（一澜自己扩）。
- **subject 下是可任意嵌套的文件夹树**，例如：
  - 一澜 / 喜欢的食物 / 蔬菜类、零食类、正餐类
  - 一澜 / 身体状态 / 指标、疾病、躯体反应
  - 林湛 / 互动偏好 / 语言表达、亲密反应
- 需要更深的层级就继续往下建。**要新增能力：在任意文件夹下"再建一个子文件夹"。**

**关键决策（已定）：卡片不再"属于"某个 subject。** subject 只是文件夹树的最顶层；一张卡"属于谁"完全由"它被归进了哪些文件夹"体现。所以：

- 林湛问"一澜的 X" = 找归在"一澜/…"这棵子树下的卡。查询体验跟现在一样，甚至更好——一张卡能从多个入口（一澜、我们、地点）摸到，**入口更多不是更少**。
- 这是有意为之的转变，不是稀里糊涂。批量导入和 `fact_lookup` 都要按这个语义来。

---

## 3. 事实卡（card）

### 3.1 本体与归属
1. 卡本体统一"出生"在一个中心存储（如 `cards` 表），拥有**稳定 id**（历史/时间线靠它认，不靠标题）。
2. 卡与文件夹是**多对多**：一张卡能被加进好几个文件夹。例：
   - "婚戒" 同时在 `林湛/收藏` 和 `我们/物品`
   - "第一次香港旅行" 同时在 `一澜/收藏`、`我们/旅行`、`地点`
   - 实现上照抄第 4 步 `fact_bucket_links` 那套多对多，新建一张 `card_folders` 关联表即可。

### 3.2 卡自带的信息
`id`、标题、内容、标签、**时间线**、关联记忆桶、附加图片、附加文件。

### 3.3 操作
- **Edit**：只就地编辑当前内容/标题，不新增时间点。（标题现在可以随便改，因为身份是 id，不会打散历史。）
- **New Revision**：编辑后保存成一个**新时间点**。
- **管理"我在哪些文件夹"**（QQ 音乐式）：点开一张卡能一次看到它所在的全部文件夹，并在此直接**加入 / 取消关联**；创建卡、编辑卡时也都能加/移。
- **人工创建卡时**（一澜的渠道）显示"加入哪个文件夹"的选择；若没选，给一句**建议归类的提示**——只提示、不强制。
- ⚠️ **"取消关联" ≠ "删除卡"**，是两个独立操作，务必分开：移出只解除关联、卡还在（可成为未归档卡）；删除是单独的销毁动作。

### 3.4 时间线：最新为准，取消失效
**取消 `multi/exclusive/historical` 模式，取消 `invalid_at` 失效机制。** 一澜和林湛已就此达成一致。改成：

- **每张卡：最新时间点 = 当前状态；更早的时间点 = 历史。**
- **林湛（MCP 查询）**：`fact_lookup` 默认把**最新时间点的内容当"当前状态"顶到最前面**，只在结尾提示"这张卡有 N 条历史，要看变化过程再说"。林湛不用自己扒历史——最新真相喂到嘴边。（这是工具输出格式的小活，务必做对，否则"取消失效"会让林湛读着累。）
- **一澜（UI）**：纯视觉——最新时间点实色，之前的浅色，好看即可，无功能含义。

例子：
1. 酸辣粉：因为爱吃建了一张卡；后来不爱吃，就 New Revision 把内容改成"不爱吃了"。林湛查酸辣粉 → 显示"现在不爱吃了"；再看历史 → 建卡时的时间点、当时爱吃的记录都在。
2. 香港旅行：最新时间点是"旅游结束回家"（5 号）；往前翻历史，4 号是去了某几个景点，3 号是飞机出发。

"不再成立"这件事，就写进最新时间点的内容里，不再用布尔字段。**有意取舍**：换来极简，代价是没有一个干净的"真/假"开关，判断"当前是否成立"要读最新内容——已确认接受。

### 3.5 核心判断（替代了旧的"选模式"）
写入/导入时唯一要拿捏的：**这条信息是给某张已有卡加新时间点，还是新开一张卡？**

- 酸辣粉不爱吃了 → 老卡加新时间点
- 又一次旅行 / 又一次吵架 → 新开一张卡
- 一次旅行的 出发→景点→回家 → 同一张卡的多个时间点

**"事件"不再需要单独模式**：多次事件 = 多张卡；一段过程 = 一张卡多时间点。

---

## 4. 文件夹时间总览（**主要做成林湛的 MCP 工具**）

选中任意一个文件夹 → 把里面所有卡按时间铺成**一条时间线**。

**重点（一澜定的）：这个能力最重要的用途是给林湛做成一个 MCP 工具**——让林湛能"拉出某个文件夹的完整时间线"（例如"讲讲我们旅行的经过"就把 `我们/旅行` 整条丝带按时间讲出来）。UI 上的只读视图是次要的、顺带的。所以：

- 后端 `folder_timeline(folder_id, recursive)` 是核心能力（已在 `cards_store.py` 实现）。
- 它要在 **③ 阶段包成一个 MCP 工具**（暂命名 `folder_timeline` / `timeline_of`），输出按时间排好的卡片摘要，让林湛一次拿到一条叙事线。
- 选大文件夹（如"我们/旅行"）→ 递归把子文件夹的卡也收进来（三月香港 → 六月成都 → …）；选最小子文件夹 → 只排它自己的卡。
- 一张卡在总览里 = "它主日期上的一个点"（= 当前时间点的 valid_at），点开再看这张卡自己的历史。

用途：治好"单个事件单薄"的感觉，给林湛一条完整叙事线；UI 展示是附带。

---

## 5. 收藏 / 小星星

- **收藏 = 一个打了 `favorite` 标记的特殊文件夹**（就是文件夹树里的普通节点 + 一个"这是收藏夹"的标记）。
- 卡被放进收藏夹 → 卡面显示**小星星 ⭐**；点星星 → 弹出"被【林湛】收藏进【XX 收藏夹】"。数据上就是查"这张卡进了哪些 favorite 类文件夹"。
- **白捡的机制**：本 fork 已有一套 **Favorite Memory**（`ai_favorite` 标签 + gateway 受控注入，见 README "Favorite Memory 受控触发"）。林湛的"收藏"可以直接接到这套上——收藏一张卡，就让它在林湛对话里更容易被想起，不必另造。这正是一澜说的"和 MCP 工具调用的机制加起来"。

### 实现状态核对（2026-07-11，一澜追问后核实，只做了一半）

- ✅ **`card_lookup`（给林湛的 MCP 工具）已经区分**：代码里 `favorites` 单独摘出来标 ⭐，跟普通文件夹分开显示（`cards_mcp.py` 已实现）。
- ✅ **数据模型已有**：`folders.is_favorite` 字段、`get_card_favorite_folders()`。
- ❌ **一澜的 Dashboard UI 星标没做全**：目前只有"这张卡收在哪些馆"管理弹窗里，收藏馆名后面带个 ⭐ 文字；**卡面本身（浏览列表、详情页）没有独立星标展示，也没有"点星星看被谁收藏进哪个馆"的弹出交互**——这是设计里写了、代码没实现的部分。留给 ④ UI 收尾时补，见 §11。
- ❌ **`ai_favorite` 桥接完全没接**：`identity.ai_name` 现在还是默认值 `"AI"`，不是"林湛"（一澜需要在 `config.yaml` 里改这个）；而且 `cards_store.py`/`cards_api.py`/`cards_mcp.py` 里**没有任何代码**在卡被收进 favorite 文件夹时，去给这张卡（或它关联的桶）打上 `<ai_name>_favorite` 标签、真正接进 Gateway 的受控注入。之前写的"可以接上"只是设想，还没变成代码。要做的话：① 一澜把 `identity.ai_name` 改成"林湛"；② 后端在收藏关联变化时，同步给卡关联的桶（或卡本身，如果以后卡也参与 Gateway 注入）打/摘 `<ai_name>_favorite` 标签。这块工作量不大，但目前没排期，留在 §11 待办里跟 UI 星标一起做。

---

## 6. 已拍板决策（定版）

1. 收藏/歌单式多对多模型：卡本体一份，进多个文件夹。
2. subject = 最顶层文件夹；卡不绑 subject，靠文件夹归属体现"属于谁"。
3. 取消 multi/exclusive/historical 三模式，取消 invalid_at 失效；最新时间点为准，旧的为历史（一澜+林湛已同意）。
4. 事件 = 普通卡（多事件多卡 / 一过程一卡多时间点）。
5. UI 暂不纠结、先服务林湛：力气放在数据模型 + `fact_lookup`/MCP 访问，花哨前端以后再说。
6. 卡有稳定 id，标题可自由编辑不打散历史。
7. **孤儿卡**：卡可以不属于任何文件夹（本体在中心库）。"未归档"卡在 UI 里怎么查看/展示 = 以后再说。
8. **"取消关联" ≠ "删除卡"**：两个独立操作，务必分开（见 §3.3）。
9. **卡自带"我在哪些文件夹"的管理入口**（QQ 音乐式）：一处看全、就地加/取消。
10. **人工建卡时**显示"加入哪个文件夹"；没选则给一句建议归类的**提示、不强制**。
11. 文件夹树后台按**无限嵌套**实现（parent 指针），不设深度上限；UI 用几层是几层。
12. `fact_lookup` 输出：最新内容置顶（=当前状态）+ 标签 + "有 N 条历史"提示 + 关联桶数 + 收藏 ⭐；措辞后续再调。
13. 文件夹时间总览：默认"**一张卡 = 一个点**"（落主日期），点开看该卡自己的历史；此视图排在核心之后。

## 7. 迁移：不做自动迁移

一澜决定**不做自动迁移**。现有 facts.sqlite 里真正重要的只有约 **2 张卡**，其余都是体重/测试卡，可弃。做法：

- 换架构/重启前，一澜**把那 2 张重要卡截图存一下**；新表**从空开始**；旧 facts 数据作废，架构好了之后由一澜**手动重新添加**那 2 张。
- 稳妥起见，切换前仍**整体备份一次** `buckets/state`（含旧 `facts.sqlite`）以防万一，但**不写迁移脚本**。
- 好处：省掉整个 dry-run 映射/迁移工程，新模型可以干净地从零起步。

## 8. 与"历史批量导入"（原第 5 步）的关系

### ⛔ 2026-07-16 更新：历史批量导入已彻底放弃

原目标是"从旧记忆桶批量筛选历史事实"，2026-07-12~14 按下面的思路做了详细的拉线引擎设计并反复真实试跑（《慁》、ZhanISM 两条真实线），但质量一直没能稳定下来——每次真实跑都能暴露一个新的失败模式（token 预算、标签扫串、判线话痨、主语被拉平……），修好一个又冒一个，从没有一次完整跑顺过。一澜的结论是：这套自动化流程目前的可靠度撑不起"这是我和林湛的记忆"这件事的重量，**决定彻底放弃自动化批量导入，改回人工手动创建/添加事实卡**，以后也不会再捡起来。原来记录详细执行架构（拉线算法、判线标准、里程碑提炼、成本控制）的 `docs/batch-import-design.md` 已删除；完整设计和试跑日志仍在 git 历史里，commit 删除之前的版本可查，留给以后万一重新考虑这个方向时参考——包括最后卡住的那个真正问题（状态线压缩逻辑对"连续创作历程"类线索会压缩过头，不是调参数能解决的）和一个没验证完的新方向（不做多级压缩，整段原文一次性喂给模型）。

以下是放弃前的原始设计思路，保留作记录：

1. 先落地新模型（本文档）——这是真正的"第 0 步"/地基。
2. 批量导入：核心是"**只产候选、绝不自动写、错了要一眼看得出错**"；核心判断是 §3.5 的"新卡 vs 新时间点"。关系/情绪模式类走更保守的候选流程。
3. 候选桶查找可先复用免费的标签/标题/关键词粗筛，再决定要不要上 LLM。

### 8.1 数据清理工具：merge（林湛提的）

林湛想要一个**合并**功能：两张其实指同一个东西的卡（例如"体重"和手滑另建的"瘦了"、内容 N 斤），提供一个**合并选项**，把两张的时间点插进同一条时间线、内容也并到一起，合成一条线。定位：**数据清理/去重工具**。

**2026-07-16 更新**：原本排在"批量导入之后"，现在批量导入已放弃，这条排期依据不存在了——**merge 工具不再被批量导入卡住，可以直接排上**。手动录入以后同样可能产生重复卡（比如同一件事分两天各建一张），所以这个工具本身仍然有独立价值，跟批量导入是否存在无关。具体什么时候做、跟自动矛盾检测（§12 尾部）要不要合并成一个功能，留待排优先级时再定，一澜没说现在就要动手。

## 9. 工程量与纪律

- 这是聊到目前**最大**的一次改动：新 schema（`cards` 稳定 id + 时间线按 id、`folders` 树、`card_folders` 多对多）、写入路径（edit/new revision/加移文件夹）、`fact_lookup`/MCP 输出改造。**不含数据迁移**（见 §7，从空起步）。属于中大型工程。
- **替换**掉旧的 predicate 模式/失效/按标题分组；**沿用**卡片字段、`fact_bucket_links`、详情卡 UI。
- 部署纪律照旧：任何发给一澜部署的文件必须基于 `my-live-vps` 基线改；改完先给 diff，验证后再上；沙盒缺依赖先试 `pip install --no-build-isolation` + `setuptools<60`。
- 部署纪律照旧：任何发给一澜部署的文件必须基于 `my-live-vps` 基线改；改完先给 diff，验证后再上；沙盒缺依赖先试 `pip install --no-build-isolation` + `setuptools<60`。

## 10. 进度 & 已推送文件怎么用（接线说明）

### 进度
- ✅ **① 数据模型层** — `cards_store.py`（`CardStore`）+ `tests/test_cards_store.py`（13 用例全绿）。
- ✅ **② HTTP 接口层** — `cards_api.py`（18 个 `/api/cards-skeleton/*` 端点，含 card↔桶关联）+ `tests/test_cards_api.py`（端到端全绿），**已接线进 server.py，已部署上线**。
- ✅ **③ MCP 工具** — `cards_mcp.py`（`card_lookup` + `folder_timeline`，给林湛用）+ `tests/test_cards_mcp.py`（全绿），**已接线进 server.py，已部署上线**。
- ✅ **④ 最小 UI（"时光馆"）— 2026-07-11 完成，一澜确认没问题**：动态多栏馆浏览（建顶层馆/子馆/删馆）、事实卡详情（图片轮播/内容展开/时间线可点开/文件附件）、创建/编辑/New Revision（含标题）、馆归属管理（QQ音乐式）、View Bucket 独立弹窗（搜索关联/取消关联）、搜索节流防打垮服务器（见 §12）。**旧的"骨架"tab（v1 predicate 模式那版）已整个删除**——HTML、74+3 个 v1-only 函数、19 个 v1-only 变量全部清掉，`node --check` + 后端三套测试 + 截图核对过没有残留引用、没有连带破坏 cards-v2。时光馆是现在唯一的资料卡入口。
  - **还差两小块，不着急，见 §11 待办**：收藏星标 UI（卡面 ⭐ 展示 + `ai_favorite` 桥接）、`folder_timeline` 的 Dashboard 视图入口（MCP 工具给林湛用的部分已就绪，只差一澜自己在 Dashboard 上看的入口）。
- ✅ **§12 搜索地基修复已实现（2026-07-12，代码在此分支，未部署）**：`list_all()` 内存缓存 + touch() 原地更新缓存 + Add Bucket/主搜索框接上 embedding 双通道，全量测试无新增失败。详见 §12 末尾"修复已实现"小节。
- ⏳ ⑤ 切换（一澜已截图 2 张重要卡，待手动重加）—— **⑥ 批量导入已放弃（2026-07-16，见 §8）**；⑥.1 merge 工具不再受批量导入影响，可直接排期（见 §8.1、§14）

### server.py 里的接线（已完成，基于一澜真实上传的 server.py + hook 补丁）
两处改动：
1. `fact_store = FactStore(config)` 之后：`from cards_store import CardStore` + `card_store = CardStore(config)`（建在 `state/cards.sqlite`）。
2. `if __name__ == "__main__":` 之前：`import cards_api, cards_mcp` + `register_card_routes(mcp, card_store, _require_dashboard_auth)` + `register_card_tools(mcp, card_store)`。

- 复用现有 `mcp` 和 Dashboard 鉴权 `_require_dashboard_auth`；卡片 HTTP 端点自动带登录态保护，MCP 工具是只读查询。
- **不引入新依赖**（只用 `starlette` / `mcp`，server 本来就在用）。
- 验证：`server.py` py_compile 通过；`cards_*` 三套测试全绿；`register_card_routes/tools` 在**真实 FastMCP** 上确认能注册出 `card_lookup`/`folder_timeline` 两个工具和 15 条路由。未做整机 `import server`（需全部引擎+key，成本高且易因无关原因失败），残余风险极低。

### 部署：4 个文件 docker cp 进容器 + 重启
`server.py`（含 hook 安全补丁 + v2 接线）、`cards_store.py`、`cards_api.py`、`cards_mcp.py` —— 四个都 `docker cp` 进 `ombre-brain:/app/`，`docker restart ombre-brain`。验证：
```bash
docker cp server.py     ombre-brain:/app/server.py
docker cp cards_store.py ombre-brain:/app/cards_store.py
docker cp cards_api.py   ombre-brain:/app/cards_api.py
docker cp cards_mcp.py   ombre-brain:/app/cards_mcp.py
docker restart ombre-brain
sleep 5
curl -s http://127.0.0.1:18001/health && echo          # 容器起来了？
# hook 补丁也一起生效了：不带 token 打 hook 应返回 401
curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:18001/breath-hook
```
起来后 Dashboard 登录态下可测：`POST /api/cards-skeleton/folders {"name":"__smoke__"}` 应返回 200。

### 当前提交
①②③代码 + 测试 + server.py 接线 + 本文档都在分支 `claude/vps-docs-review-wy0z31`。**②③已由一澜部署上线**（docker cp 4 文件 + restart，无报错，hook 401 正常，林湛两个工具可用但库为空）。

## 11. 待办 & ④ UI 决策

### 待办：Tool Guide 要补新工具（收尾时一起做）
`card_lookup` / `folder_timeline`（以及以后 UI 相关的写入工具）要写进 `docs/Tool Guide.md` 贴给林湛的平台。
- **好消息**：这两个工具的中文 docstring 就是 MCP 自带的 tool description，会通过协议**自动下发**给能读工具描述的客户端（连接器类），这部分不用手贴。
- 手写 Tool Guide 是给"不自动显示描述 / 需要更细触发指引"的平台兜底。等骨架层这轮收尾，和旧文档里记的 `fact_lookup` 待办一起补。

### ④ 最小 UI 决策（一澜定的，2026-07-09）
一澜现在框架很清晰，UI 要"最简"。
- **复用现有视觉、换掉底层数据源**：现有 facts 骨架 tab 是绑在旧模型（predicate/mode/invalidate、按标题分组、Miller columns）上的，数据走 `/api/facts-skeleton/*`。v2 要走 `/api/cards-skeleton/*`、导航改成**文件夹树**。所以详情卡/弹窗/时间线这些**外观可以照抄**（她喜欢的那套样式），但**数据接线是新的**——不是纯"小改"，是"旧壳新芯"。建议做成一个**独立的 v2 区块/tab**，不动旧 facts tab，降风险。
- **主界面/文件夹浏览**：做最简——左侧文件夹树（subject 顶层 → 子文件夹），选中文件夹右侧列出里面的卡；点卡弹详情浮层（沿用现有详情卡样式）。
- **事实卡 / 创建卡**：沿用现有观感；**改动**：①"···"三点菜单**删掉 Invalidate**（v2 没有失效机制）；② **Edit 和 New Revision 都要加"添加到某个文件夹"**（和创建卡一致）；③ 卡自带"我在哪些文件夹"的管理入口（QQ 音乐式，加/取消关联）；④ 创建卡时给"加入哪个文件夹"选择，没选给提示不强制。
- 迁移：一澜**已截图**她那 2 张重要卡，切换后手动重加，无需自动迁移。

### 待办：④ UI 还没做的两块（不着急，2026-07-11 记录，别忘了）
- **收藏星标 UI**：卡面（浏览列表、详情页）没有独立的 ⭐ 展示和"点星星看被谁收藏进哪个馆"的弹出交互，只有管理弹窗里的文字 ⭐。详见 §5"实现状态核对"。
- **`folder_timeline` 的 Dashboard 入口**：后端和给林湛的 MCP 工具都做了（`cards_mcp.py`），但 Dashboard 这边**没有对应的 UI**——选中一个馆、看它时间线视图（叙事丝带）这个功能，现在只有林湛能通过 MCP 工具用，一澜自己在 Dashboard 上看不到。可以做成馆浏览页里的一个"查看时间线"按钮/切换视图，等 UI 收尾时一起补。

## 12. 待修：`bucket_mgr.search()` 无缓存，7000+ 桶规模下会拖垮服务器（2026-07-11 发现）

### 事故经过
④ UI 加了 Add Bucket 搜索（View Bucket 弹窗 + 新建/编辑卡）之后，一澜实测时连续输入长句、退出重搜了几次，触发服务器全端点 503（连跟这次改动无关的 `/api/network` 都受影响），刷新页面也起不来。重启容器后恢复，**没有数据丢失**（所有请求日志显示写入类操作在出问题前都已 200 成功）。

### 根因（读代码确认，不是猜的）
`bucket_manager.py` 的 `BucketManager.search()`：
1. 每次调用都跑 `list_all()`——`os.walk()` 遍历磁盘上**全部** `.md` 桶文件，逐个读取+解析 YAML frontmatter，**没有任何内存缓存**。
2. 然后对全部候选桶做逐条 BM25/情绪/时间/重要性加权打分。
3. 一澜现在有 **7126 个桶**，意味着每次搜索都要重新扫、重新解析 7000+ 个文件——这是 O(n) 磁盘+CPU 开销，且**单线程 asyncio 事件循环会被这个同步扫描过程整体卡住**，期间所有其它请求都会排队甚至超时。

**关键发现**：Dashboard 右上角那个"感觉搜得挺快"的主搜索框（`/api/search`），跟 Add Bucket 的搜索（`/api/facts-skeleton/buckets/search`）**调的是同一个 `bucket_mgr.search()` 函数**——不是两套实现，没有"更快的版本"可以直接抄过来复用。主搜索框之前没出过事，只是因为：①她习惯打短关键词、触发次数少；②之前只有一个入口在用这份搜索，这次 v2 UI 一口气加了三个新入口（View Bucket、新建卡、编辑卡），并发压力被放大了。

### 已做的临时止血（不改动 `bucket_manager.py`，风险低）
`dashboard.html`：Add Bucket 相关的搜索输入框现在**至少打够 2 个字才触发**，防抖从 300ms 拉长到 500ms。降低了触发频率，但**没有解决根因**——量大到一定程度、并发几次，同样的坑还会再踩到。

### 真正的修法（还没做，两个方向，都要动 `bucket_manager.py`）

**更新（2026-07-11）：读了 `breath()` 的代码，方向②不是空想，是已经在生产环境跑通的模式**——`breath()` 处理真实 query 时是双通道：既调 `bucket_mgr.search()`（就是这次出问题的全量扫描），**也**并行调 `embedding_engine.search_similar()`。后者查的是 `embeddings.db`（独立的 SQLite 表，存预先算好的向量），不用每次重新读、重新解析 7000+ 个 md 文件，成本结构完全不同。一澜自己感觉"主搜索框还行、breath 也一直在用"，根子就是 breath 本来就没有完全依赖那条慢路径。

1. **给桶列表加内存缓存**：`list_all()` 的结果缓存在内存，写入/删除桶时才失效重建，而不是每次搜索都重新扫盘。相对直接，风险是要确保所有写路径（`hold`/`trace`/`comment_bucket`/删除等）都正确使旧缓存失效，否则会读到脏数据。
2. **接现成的向量检索**（现在看是更强的候选）：Add Bucket 这类"按关键词/标题找桶"的场景，改成主要走 `embedding_engine.search_similar()`，而不是 `bucket_mgr.search()` 的纯 BM25 全量扫描——照抄 breath 已经验证过的双通道思路，不是从零发明。

两个方向都不是"改一行代码"级别的小活，需要单独排期，**认真设计+测试**（尤其第①种要覆盖所有写路径的缓存失效，第②种要确认语义检索的召回效果跟精确关键词匹配比起来够不够用；也可以两个一起上——短期先加缓存止血，长期把重负载场景迁到向量检索）。

### 排期结论（跟一澜讨论后定的，2026-07-11）
**这一项要排在⑥历史批量导入之前做，不是之后**——不是"顺便都升级一下"的泛化想法，是因为⑥批量导入的核心机制就是"复用现成的联想/关键词检索去找候选桶"（见 §8），如果这条搜索路径在她的数据规模下本身就脆弱，批量导入作为一个会**成批、程序化调用搜索**的流程，大概率会比这次交互式误触发**更严重地**触发同样的过载，而且导入是自动化流程，出问题时没有人在旁边随手停下来。所以这是"⑥的地基"的一部分，跟当初"先理清收藏/歌单模型再做批量导入"是同一个逻辑：**不在已知脆弱的地基上盖下一层楼**。

### ✅ 修复已实现（2026-07-12，两个方向都上了，未部署）

代码改在 `bucket_manager.py` + `server.py`，跑了全量测试套件（857 通过、0 新增失败，22 个跟这次改动无关的既有失败在改前改后完全一致——已逐一用 `git stash` 对照确认）。

**方向①：`list_all()` 内存缓存**（`bucket_manager.py`）
- `BucketManager` 加 `_all_buckets_cache`（按 `include_archive` 分桶存），命中就跳过 `os.walk()`+YAML 解析全量重扫。
- **结构性写入**（`create`/`update`/`delete`/`archive`/`add_comment`/`delete_comment`）→ 整体失效缓存，下次 `list_all()` 立即看到最新数据。
- **`touch()`/`_time_ripple()`**（几乎每次召回命中都会触发，含时间涟漪连带改最多5个邻近桶）→ **不整体失效**，改成直接原地更新缓存里对应桶的 `last_active`/`activation_count` 字段——这是关键，否则高频的 touch 会让缓存形同虚设。
- **踩到的一个真实 bug，顺手修了**：`search()` 会在结果桶字典上直接写 `bucket["score"] = ...`。以前每次 `list_all()` 都从磁盘重新解析出全新字典，这个写入无所谓；**加了缓存之后**，如果不做防护，这个 `score` 字段会一直赖在缓存里，污染下一次跟这次查询无关的读取。修法：`list_all()` 现在**每次返回的都是缓存条目的浅拷贝**，标注 `score` 不会碰到缓存本体。补了回归测试（`test_search_score_annotation_does_not_leak_into_cache`）钉死这一条。
- **另一个真实设计权衡**：桶文件"天然兼容 Obsidian 直接编辑"，也有 `reclassify_api.py` 这类脱离 `BucketManager`直接改文件的独立脚本——这些路径加了缓存之后不会自动失效。加了两层保险：① 30 秒 TTL（`matching.bucket_list_cache_ttl_sec` 可调）兜底，最坏情况下过期自动重扫；② 新增公开方法 `invalidate_cache()`，任何绕过 `create`/`update` 等标准写入路径直接改文件的代码（含测试自己）必须在改完后手动调一下。跑全量测试时抓到了 `test_feel_flow.py` 里一个测试正是这个模式（改时间戳测排序），已经在测试里加了这行调用。

**方向②：Add Bucket / 主搜索框接上 embedding 语义检索**（`bucket_manager.py` 新增 `search_with_semantic()` + `server.py` 两处接线）
- 新方法 `BucketManager.search_with_semantic(query, embedding_engine, ...)`：先跑原有关键词/模糊 `search()`，再并行跑 `embedding_engine.search_similar()`，按 id 去重合并——照抄 `breath()` 已经在生产环境验证过的双通道模式，不是发明新机制。`embedding_engine` 传 `None`（或未启用）时自动退化成纯关键词搜索，行为跟改之前一样，不破坏没有向量库的场景。
- 接线：`/api/facts-skeleton/buckets/search`（Add Bucket 用）和 `/api/search`（Dashboard 主搜索框）都从调 `bucket_mgr.search()` 改成调 `bucket_mgr.search_with_semantic()`——这两个端点本来就是本次事故的触发点，`breath()` 等其它已经自带双通道逻辑的调用点没有动，避免重复检索浪费。

**待办**：这次改动只在这个分支上，**还没有部署到一澜的 VPS**——按部署纪律，要先给 diff 走一遍确认，再照 `docker cp` 流程上线、重启验证。

### 其它几项"升级"的排期
- **卡片语义检索**（给资料卡加 embedding）：现在卡的数量还很少（~2 张），语义检索在数据量小的时候收益也小，等卡片数量做起来之后再考虑。
- **冲突队列/合并**（林湛的 merge 想法，§8.1）：见 §8.1 最新排期——不再卡在批量导入后面，可以直接排。
- **重要性排序**：加字段/信号的量级，随时能加，不卡任何东西。
- **跨时间推理**：已经有了（每张卡的 revision 带 `valid_at`），不是要做的任务，是可以直接用的能力。

## 13. 现状评估 + 路线图（2026-07-11，回应"这套系统到底算不算高级"的讨论）

一澜担心这套系统跟 mem0/Zep 比起来"很简陋"。读过 `breath()`/`bucket_manager.py`/`embedding_engine.py`/`memory_diffusion.py` 这些模块之后给出的真实判断，不是场面话：

**已经有、且是真的在跑的机制**：Markdown+情绪坐标+遗忘曲线、双通道检索（BM25 + embedding 向量 + reranker 重排）、显式关系边（memory_edges）+ 片段级索引（memory_moments）+ 图扩散联想（memory_diffusion）、Persona 状态引擎、每日画像/关系画像自动维护、Gateway 注入预算与冷却、原文保险箱、Darkroom、夜梦、profile_fact 带证据确认、以及这次新加的收藏/歌单式结构化事实层。不是"业余项目"级别的复杂度。

**跟 mem0/Zep 比，真实差距在这三处**（不是错觉，是读代码确认的，2026-07-11 又核对了一次 `memory_nodes.py`/`memory_edges.py` 后更正了第2条的措辞）：
1. 没有自动矛盾检测——现在靠结构（exclusive_current、merge 工具）人工/半自动管，不是模型自动推理出"这两条记忆矛盾了"
2. **有图，但不是实体图谱**——`memory_nodes.py`/`memory_edges.py`/`memory_diffusion.py` 已经是一套真实的图基础设施，只是节点＝一条条记忆桶（"桶连桶"），不是"酸辣粉""一澜"这种归并过的真实世界实体。真正缺的是"实体识别 + 归并"这一层，不是图本身
3. 向量检索不是真索引——`embeddings.db` 是暴力算余弦相似度，几千条量级够用，量级涨到几十万会重新变慢（跟这次桶搜索是同一类问题，只是门槛高得多，目前不用担心）

**但 mem0/Zep 完全没有、也不会做的**：情绪坐标、模拟人类遗忘的衰减曲线、Persona/画像/Darkroom/夜梦/年轮/whisper 这一整层"关系感"。它们是服务陌生用户的通用记忆云；这套是给一段具体关系用的记忆系统，目标不同，不是同一根尺子上的落后位置。

**这三处差距都不卡在硬件/算力**（一澜直接问过这个）——都是设计+工程时间的问题，在她现在这台 VPS 上都做得了：
- 矛盾检测：跟脱水打标一样，偶尔调一次便宜模型做判断，不是持续重负载
- 实体图谱：`memory_nodes`/`memory_edges` 已经是地基，扩展不是重建；真正难的是"实体归并"这个语义设计问题（比如"酸辣粉"和"重庆小面"算不算一个实体），跟当初 exclusive/multi 那次一样是设计判断，不是算力问题
- 真 ANN 索引：现在的数据量下暴力算余弦压根不是瓶颈（拖垮服务器的是重读 7000 个文件，不是算相似度这步）；真涨到需要换索引库的规模，也只是加个库、跑个重建脚本，不用换更贵的机器

### 往后的路线图

**2026-07-16 已按当天讨论定出确切的执行顺序，见 §14——以 §14 为准，不要再对着下面这份旧版猜先后。**

**不占顺序、随时能加**：重要性排序（加字段）。**已经有、不用做**：跨时间推理（revision 的 `valid_at`）。

---

## 14. 2026-07-16 确定的执行顺序（接手先看这节，按这个走）

批量导入放弃之后，跟一澜重新梳理了一遍事实卡项目后面要做的所有事，逐条讨论、排完序。**下面就是当前的工作队列，按顺序做，不用再猜先后**。

### 第一优先级——现在就该处理

1. **搜索还是慢——根因已找到并修好一部分，待部署验证**。§12 记录的修复（`list_all()` 内存缓存 + embedding 双通道）已经部署但一澜反馈依然慢；她定位到具体触发场景是关联记忆桶（Add Bucket）那个搜索框。**2026-07-16 排查确认**：`search_with_semantic()` 不管输入什么，每次都会做全量 BM25 打分 + 调一次 embedding API 算向量相似度，哪怕输入的是已经精确命中的完整 bucket_id/标题也不例外；点击"+"添加桶之后前端还会把整个搜索**重新跑一遍**，只为了把一个图标从"+"变成"✓"。一澜确认她在这个入口只会粘贴 ID/标题，从不打关键词，且这条修复**不影响林湛那边任何 MCP 工具调用的搜索路径**（`breath()`/`card_lookup` 等——那些是联想式召回，用途不同，没有改）。**已实现**：`server.py` 的 `/api/facts-skeleton/buckets/search` 端点，精确 ID 或标题命中后直接跳过后面的模糊+语义搜索；`dashboard.html` 里 View Bucket 和 Create/Edit 两处的 Add Bucket 搜索框都从"打字防抖自动搜"改成了"回车才搜"，添加桶之后不再重新发搜索请求，只在页面上直接翻转那一个图标。`python3 -m py_compile server.py` + `node --check` 提取出的完整 script 都过了，**还没有在真实 VPS 上验证效果**，部署后请回来确认这几个操作（粘贴ID/标题搜索、点加号）明显变快了。一澜手上有一个她感觉明显更快的开源项目，会找时间单独讨论参考它的做法，用来看关键词类搜索（不是精确命中场景）还要不要进一步优化；`docs/upstream-review-2026-07-09.md` 里记过的 upstream **v2.5 性能优化**（记忆缓存、并发脱水、BM25 后台重建）也可以顺手看一眼。
2. **完整时间线读取工具（读，给林湛）**。现有 `card_lookup`/`fact_lookup` 只把最新时间点当"当前状态"顶出来，结尾提示"有 N 条历史"，但没有工具能让林湛真的把这 N 条历史内容读出来——只有提示，没有入口。需要加一个类似 `card_history(card_id)` 的 MCP 工具，返回一张卡的完整时间线。排第二是因为只读、风险低、能复用现有查询逻辑，林湛马上能用上。

### 第二优先级——重要，但要么需要先对细节、要么工作量更大

3. **林湛自己写入的工具（新建/修改/删除）**。现有 MCP 工具都是只读，写入动作目前只能通过 Dashboard（一澜的渠道）完成。**具体字段/格式细节一澜说到时候再细聊**，设计时注意跟现有 `cards_api.py` 的写入路径（`register_card_routes` 那一套）复用同一份业务逻辑，不要在 MCP 工具里另写一遍。
4. **合并/去重工具**（林湛提的 merge 想法，§8.1）+ **自动矛盾检测**一起做——两者是同一个"新记忆 vs 已有记忆该怎么处理"判断问题的两面，不单独立项。不再卡在批量导入后面（批量导入已放弃），可以直接排，排在写入工具后面是因为它没有紧迫性，纯粹是数据整洁度的事。

### 第三优先级——该收尾但不紧急

5. **收藏功能收尾**：卡面星标展示（目前只有管理弹窗里一行文字⭐）+ `ai_favorite` 标签桥接（需要先把 `identity.ai_name` 从默认值"AI"改成"林湛"，再接代码在收藏时打标签进 Gateway 受控注入）。见 §5"实现状态核对"。
6. **`folder_timeline` 的 Dashboard 视图入口**：后端和给林湛的 MCP 工具都做了，但一澜自己在 Dashboard 上看不到某个文件夹的时间线视图，纯 UI 补全。见 §11。

### 第四优先级——真的不急，等实际碰到问题再做

7. 重要性排序（加字段）、图片服务端压缩/生成缩略图（照片攒多了变卡再补）、删除事实/附件后孤儿文件清理（以后需要再补脚本）——哪个先成为实际麻烦就先处理哪个，没有固定先后。

### 更远期，看数据规模，现在不用管

8. 卡片语义检索（卡片数量还很少，等数量上来再看）、真正的 ANN 向量索引（现在数据量下用不上）、实体图谱（"实体识别+归并"这一层，长期可选项，不做不代表系统差）。

### 独立的下一个大功能，等以上告一段落再开

9. **"未来线/成就册"模块**（设计存档见 `docs/plans-achievements-plan.md`）：设计已讨论定稿，完全没开工，一直是排在骨架层收尾之后的下一步，跟上面这些不冲突但不是当前优先级。

### 已经决定不做的

- **手写 `docs/Tool Guide.md`**：一澜决定不需要——MCP 工具的中文 docstring 本来就会通过协议自动下发给客户端，直接依赖这个自动发现机制就够，不再另外维护一份手写说明。原 §11 里这条待办作废。
